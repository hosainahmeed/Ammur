import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["fakeimg.pl", "avatar.iran.liara.run", "amurphy-bucket.s3.us-east-1.amazonaws.com"],
  },
};

export default nextConfig;
